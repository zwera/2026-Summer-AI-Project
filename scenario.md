# 사용자 시나리오: 경찰 판례·법령 AI 봇

## 시나리오: "야간 주거 침입 신고 현장에서의 긴급체포"

### 1. 진입 — 범위와 고지 확인 (요구사항 1, 12, 14)
김 경장이 배포된 URL로 접속합니다. 상단 고정 영역에 이렇게 보입니다.

- `목업 응답` 표지
- 데이터 기준일: `2025-06-30` / `실시간 판례·법령 동기화 없음`
- 목표 데이터 범위: `1심 판례 전체` / 현재 구현 범위: `사전에 정의된 목업 1심 판례 샘플`
- 고지문: "이 웹 애플리케이션은 사전 정의 목업 데이터 기반 시연이며 법률 자문이나 실제 수사·업무 판단을 제공하지 않습니다."

### 2. 상황 입력 — 현장 표현 그대로 (요구사항 2)
사건번호나 죄명 대신 평소 말투로 입력합니다.

> "새벽에 남의 집 문 따고 들어가려던 사람을 그 자리에서 잡아서 영장 없이 데려왔어요"

앱은 이 문장을 정규화해 `QueryFixture` 키와 정확히 대조합니다(임베딩·퍼지 검색 없음). 지원 질의로 판정되면 화면에 `원문 ↔ 법률 검색어` 대응표가 나옵니다.

| 현장 표현 | 법률 검색어 |
|---|---|
| 문 따고 들어가려던 | 주거침입 실행의 착수 |
| 그 자리에서 잡아서 | 현행범/긴급 체포 |
| 영장 없이 데려왔어요 | 영장 없는 체포 |

관계 보존 상태도 함께 표시됩니다. "사람 → 침입 시도", "체포 → 새벽(시점)", 부정 표현 "영장 없이 → 체포" 관계가 변환 전후 동일하게 유지됨이 확인됩니다.

만약 "데려왔다"가 임의동행인지 체포인지 하나로 확정되지 않으면 검색 버튼이 비활성화되고 원문과 함께 `해석 확인 필요`가 표시됩니다.

### 3. 목업 RAG 단계 진행 (요구사항 1, 13)
검색을 수락하면 화면 stepper와 스크린 리더 상태 메시지가 순서대로 진행됩니다.

`입력 ✓ → 목업 검색 ✓ → 근거 제시 ✓ → 응답`

각 단계는 완료된 다음 단계만 활성화됩니다. 이 과정에서 외부 검색 엔진·API·LLM 호출은 0건이고, 오직 fixture ID 조회와 순수 함수만 실행됩니다.

### 4. 결과 목록 — 유사도 순 정렬 (요구사항 3, 4, 7, 10)
`긴급체포` 시나리오로 분류된 판례 카드가 사전 정의된 검색 우선순위대로 나옵니다.

- **판례 A** (유사도 92, 적법, 현행법 기준) — 새벽 주거침입 착수 직후 현행범 체포를 적법으로 본 1심
- **판례 B** (유사도 74, 판단 혼재, `구법 기준` 배지) — 체포는 적법하나 이후 물리력은 위법으로 본 사례
- **판례 C** (유사도 41, 위법) — 침입 착수가 인정되지 않아 체포를 위법으로 본 사례

같은 우선순위는 `tieOrder`, `caseId`로 안정 정렬되고, 현행법 기준 판례가 구법 기준보다 항상 앞섭니다. 각 카드에는 사건번호·법원명·1심·선고일·시나리오·유사도·적법성·법령 상태·최종 인정 죄명·실제 재판 결과가 표시됩니다. 결과 영역에는 "유사도 점수와 검색 순서는 사전 정의된 목업 값이며 운영 산식 또는 정확성 보증이 아닙니다."라는 문구가 붙습니다.

### 5. 유사도 경고 — 숫자보다 차이가 먼저 (요구사항 8)
판례 A는 유사도 92지만, 결론을 바꿀 수 있는 핵심 차이가 경고 영역 맨 위에 배치됩니다.

> ⚠ `높은 유사도 — 핵심 차이 확인 필요`
> - 사용자 사실: 침입 "시도" 단계 / 판례 사실: 침입 후 "물건 물색" 단계 / 결론 영향: 착수 시점 차이가 현행범 성립에 영향

사용자 사실이 질의에 없으면 `확인 필요`로 표시됩니다.

### 6. 판례 상세 — 단계별 요약과 전문 대조 (요구사항 3, 5)
판례 A를 열면 `요약 / 개인 책임 위험 / 핵심 사실 차이 / 법령 상태 / 상급심 / 전문` 섹션이 나옵니다. 3줄 요약을 선택하면:

1. 사건 개요: 새벽 주거 침입 착수자를 현장에서 체포한 사건
2. 법원 결론: 현행범 체포 요건을 갖춰 적법
3. 현장 경찰 핵심 포인트: 착수 시점과 즉시성이 적법성의 핵심

10줄·상세로 바꿔도 결론·적법성·죄명·결과 같은 정규 결론은 동일하게 유지됩니다. 법률 용어 "실행의 착수"는 최초 설명 위치에서 현장 표현과 함께 병기됩니다. 전문은 접힌 상태이고, 요약 문장의 출처 링크를 누르면 전문이 열리며 해당 구절로 이동·강조된 뒤 "인용으로 돌아가기"로 복귀합니다.

### 7. 개인 책임 위험 배지 (요구사항 6)
판례 B 상세에서 위험 4축이 각각 표시됩니다.

- 민사 국가배상: `국가배상 기각`
- 형사 직권남용: `정보_없음`
- 형사 독직폭행: `해당` (이후 과도한 물리력)
- 징계: `분류_불가` (출처 간 판단 충돌)

행동별로는 "체포 행위 → 적법_행동" 배지, "제압 물리력 → 문제_행동" 배지가 색상 + 문구 + 아이콘으로 구분됩니다.

### 8. 선택 영역 재검토 (요구사항 9)
김 경장이 응답 중 "영장 없이도 적법하다"는 문장을 드래그하면 `사실 확인 재검토`와 `상세 설명` 메뉴가 뜹니다. 재검토를 실행하면 독립 주장별로:

- 주장: "현행범 체포는 영장 없이 가능" → `근거_일치` (결정 근거: 판례 A 출처 #3)
- 참고 출처는 별도 영역에 분리 표시

### 9. 법령 상태와 상급심 (요구사항 10, 12)
판례 B는 `구법 기준` 배지와 개정 설명이 붙습니다. 상급심 정보가 있으면 항소심 사건번호·결과가 나오고, 1심과 결론이 달라진 부분은 강조됩니다. 정보가 없으면 추론하지 않고 `정보_없음`으로만 둡니다.

### 10. 타임라인과 보고서 재사용 (요구사항 11)
사전 정의 음성 시연 항목을 선택하면 인식 텍스트가 재생되고(실제 음성 인식 0건), 뒤섞인 서술이 시간 순으로 정렬됩니다.

​```
02:10  주거 침입 착수 발견        (쟁점: 현행범 요건)
02:11  현장 체포                 (쟁점: 영장주의 예외)
02:40  경찰서 도착               (연결 쟁점 없음)
[시점 미상]  진술거부권 고지        (사용자 확인 필요)
​```

각 이벤트를 수정하면 타임라인과 보고서에 즉시 함께 반영됩니다. "보고서용 사실관계 생성"을 누르면 타임라인 순서 그대로 텍스트가 만들어지고, 데이터 기준일과 법률 안전 고지문이 본문에 포함된 채로 클립보드 복사 또는 `.txt` 다운로드가 됩니다. 복사가 실패해도 본문과 타임라인은 화면에 유지되고 수동 복사 안내가 나옵니다.

### 11. 안전한 실패 (요구사항 13)
만약 판례 C의 출처 참조가 깨져 있으면, 그 레코드만 격리되어 `출처 데이터 오류`가 표시되고 나머지 유효한 판례 A·B는 그대로 유지됩니다. 오류가 나도 목업에 없는 새 법률 결론을 지어내지 않으며, 오류 전 상태(질의·선택 판례·요약 단계·필터)는 보존됩니다.

---

이 흐름은 `tasks.md`의 구현 순서와도 맞물립니다. 데이터셋 검증(태스크 2) → 질의 해석(3) → 목업 검색·정렬(5) → 응답·출처(6) → 요약·배지·경고·타임라인(8–13) → 오케스트레이터·서버·클라이언트(15–22) 순으로 쌓으면 위 시나리오가 그대로 동작하게 됩니다.
